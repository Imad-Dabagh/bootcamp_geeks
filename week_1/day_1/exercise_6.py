# ------ Exercise 6: Tuple
# We can't modify a tuple directly after creation, but we can create a new one by concatenating the original tuple with the tuple of integers.
typle = (10, 20, 30)
new_tuple = typle + (40, 50, 60)
print(new_tuple)
